# Restserver with typescript and mysql

## BBDD credentials
### user: root
### password: example

1. Clone project

2. ```npm install```

3. Run BBDD
```docker-compose up -d```

4. Run TSC watch in one terminal in the same folder
```tsc --watch```

5. Run the app in dev
```nodemon dist/app.js```